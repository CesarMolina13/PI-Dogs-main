// if(!input.temperament.includes(input.select1)){
//     temperam.push(input.select1) 
//    }
// }
// if(input.select2.length>0){
   
//     if(!input.temperament.includes(input.select2)){
//         temperam.push(input.select2) 
//     }
//  }
//  if(input.select3.length>0){
   
//     if(!input.temperament.includes(input.select3)){
//         temperam.push(input.select3) 
//     }
//  }
//  if(input.select4.length>0){
   
//     if(!input.temperament.includes(input.select4)){
//         temperam.push(input.select4) 
//     }
//  }
//  if(input.select5.length>0){
   
//     if(!input.temperament.includes(input.select5)){
//         temperam.push(input.select5) 
    // }

// select2:"",
// select3:"",
// select4:"",
// select5:"",


// select2:"",
//         select3:"",
//         select4:"",
//         select5:"",

// select2:"",
//         select3:"",
//         select4:"",
//         select5:"",


{/* <select name="select2" onChange={handleChange}> <option value="">choose a Temperament</option>
{
    temperaments.map((tem)=>(
        <option key={tem.name} value={tem.name}>{tem.name}</option>
    ))
}
</select>

<select name="select3" onChange={handleChange}> <option value="">choose a Temperament</option>
{
    temperaments.map((tem)=>(
        <option key={tem.name} value={tem.name}>{tem.name}</option>
    ))
}
</select>

<select name="select4" onChange={handleChange}> <option value="">choose a Temperament</option>
{
    temperaments.map((tem)=>(
        <option key={tem.name} value={tem.name}>{tem.name}</option>
    ))
}
</select>

<select name="select5" onChange={handleChange}> <option value="">choose a Temperament</option>
{
    temperaments.map((tem)=>(
        <option key={tem.name} value={tem.name}>{tem.name}</option>
    ))
}
</select> */}